/*
* GET       => http://domain.com/register?username=crazycoder&password=123
* POST      => http://domain.com/register
* PUT       => Updating
* DELETE    => Deleting
*/


/*

    ? (Non-compulsory) ~ con(ta)?ct
        => Lack of space in parentheses is not a problem
    * (Any expression may come instead) ~ cont*ct
        => We can write whatever we want instead of the star
    + (It should be the same as the one on the left) ~ conta+ct 
        => We can add as many 'a' as we want

*/